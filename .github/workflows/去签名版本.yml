name: Build Release Android (Fixed Path)

on:
  workflow_dispatch: # 手动触发

jobs:
  build-android-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master

      - uses: actions/setup-java@v4
        with:
          distribution: "zulu"
          java-version: "17"

      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Get Dependencies
        working-directory: ./simple_live_app
        run: flutter pub get

      # --- 修复点：调整证书生成路径 ---
      - name: Generate Keystore & Properties
        working-directory: ./simple_live_app/android
        run: |
          # 1. 重点修改：把 upload-keystore.jks 生成到 app/ 子目录下
          # 这样 build.gradle 在 app 目录运行时就能直接找到了
          keytool -genkey -v -keystore app/upload-keystore.jks \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -alias upload -dname "CN=Android,O=Android,C=US" \
            -storepass 123456 -keypass 123456

          # 2. 生成 key.properties (这个文件留在 android/ 根目录不动)
          echo 'storePassword=123456' > key.properties
          echo 'keyPassword=123456' >> key.properties
          echo 'keyAlias=upload' >> key.properties
          # 指向文件名即可，因为文件现在就在 app 目录里
          echo 'storeFile=upload-keystore.jks' >> key.properties

      - name: Build Android APK (Release)
        working-directory: ./simple_live_app
        run: flutter build apk --release

      - name: Upload Release APK
        uses: actions/upload-artifact@v4
        with:
          name: simple-live-release
          path: simple_live_app/build/app/outputs/flutter-apk/app-release.apk